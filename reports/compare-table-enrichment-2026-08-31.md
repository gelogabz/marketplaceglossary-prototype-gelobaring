## Compare Table Deep Pass — 2026-08-31

Ran 5 parallel review passes (procurement/fundamentals, cosell, billing, advanced, operations) over all 234 non-integration singleton terms (terms not currently in any of the 25 existing comparison rows), cross-checked against the live union-find logic in `comparison/comparison.js` and verified every candidate below directly against `data/terms.js` — not just trusting agent claims.

### Ready to apply (8 items, high confidence, independently cross-checked)

**Gap-fills into existing rows:**

1. **Committed Spend Drawdown** (row 7) — add `Commit Drawdown — Suger`. Its def is literally Suger's own explainer of this exact mechanic ("how a marketplace purchase retires a customer's committed-spend agreement — AWS EDP/PPA, Azure MACC, or GCP CUD"), already `Related:`-linked to all three but never using the `equivalent:` keyword that triggers grouping. Fix: change its alias to use `"AWS equivalent: Enterprise Discount Program (EDP) — AWS"`.
2. **Marketplace Seller Portal** (row 4) — add `Suger Console`. Its own def says "across all connected platforms" — it's the multi-cloud seller console, same functional role as AMMP/Partner Center/Producer Portal. AMMP's alias already name-drops it under `Related:` (verified live) but never as `equivalent:`. Fix: change `Suger Console`'s alias to `"Suger equivalent"` framing, e.g. add `"AWS equivalent: AWS Marketplace Management Portal (AMMP) — AWS"`. (Verified: the row's `CONCEPT_OVERRIDES` entry already forces the label to stay "Marketplace Seller Portal" regardless of Suger joining — no override change needed.)

**New rows:**

3. **Virtual Machine Image Listing** — `Amazon Machine Image (AMI) — AWS` + `Azure Virtual Machine Offer — Azure`. Both are the platform's core VM-image-based listing type (as opposed to SaaS). Wiring: shared `group: "vm-image-listing"`. Needs a `CONCEPT_OVERRIDES` entry (`"Amazon Machine Image (AMI) — AWS": "Virtual Machine Image Listing"`) — the raw AWS name leaks "Amazon."
4. **Committed Base + Metered Overage Pricing** — `SaaS Contracts with Pay-As-You-Go (Overages) — AWS` + `Flat Rate Pricing — Azure` + `Combined Pricing — GCP`. All three: fixed/committed base fee plus metered overage above an included quantity. (Two agents independently found this; one proposed the generic `Contract — AWS` term instead of the more precise overage-specific AWS term — went with the more precise one after checking both defs.) Wiring: shared `group: "committed-base-overage-pricing"`.
5. **Time-Limited Trial Listing** — `SaaS Free Trial — AWS` + `Limited Trial Listing — Snowflake`. Both: time/consumption-bounded trial access, no upfront payment, auto-expires or converts to paid. Wiring: shared `group: "time-limited-trial"`.
6. **Partner Competency Program** — `AWS Specialization — AWS` + `Solutions Partner Designation — Azure` + `Google Cloud Partner Network Competency — GCP`. Verified via full def text: all three are explicitly the platform's validated-expertise program that *replaced* a prior tier system (Azure: "replacing Gold and Silver competencies retired September 2022"; GCP: "The 2026 replacement for GCP Specializations"), and all three influence marketplace listing visibility, co-sell recommendations, and funding access. Wiring: shared `group: "partner-competency-program"`. Needs a `CONCEPT_OVERRIDES` entry (`"AWS Specialization — AWS": "Partner Competency Program"`) — raw name leaks "AWS."
7. **Partner-Led Co-sell Tier** — `Partner-Led Opportunity — AWS` + `Partner-led Deal — Azure`. Both: the co-sell tier where the ISV/partner runs the deal independently with no active field-seller engagement, while the deal stays co-sell-credit eligible. Wiring: shared `group: "partner-led-cosell"`.
8. **Pre-Launch Restricted Listing Preview** — `Limited Release — AWS` + `Preview Audience — Azure`. Both: an allowlist of specific accounts/tenants that can see and transact a not-yet-public listing, used to validate the purchase/entitlement flow before a distinct "go public" action. Wiring: shared `group: "prelaunch-restricted-preview"`.

None of items 4/5/7/8 leak a brand name in their natural label (checked against the `BRAND_LEAK_RE` guard), so no override strictly required for those — I'd still add clean overrides for polish, matching the existing rows' style, unless you'd rather keep the raw term names.

### Flagged — needs your call before I touch anything

- **Two likely duplicate-term pairs, not comparison gaps.** `Selling Authorization — AWS` and `Resale Authorization — AWS` are already cross-referenced as `"Also called:"` each other — same AWS feature (UI name vs. API name), not two platforms. Same situation with `Private Pricing Agreement (PPA) — AWS` and `Enterprise Discount Program (EDP) — AWS` (`"Also known as:"` each other). Both pairs are already correctly synonym-linked and don't need comparison-table wiring — but having two separate glossary *entries* for the same single-platform concept might be worth consolidating in a future cleanup pass. Not touching this now since it's outside today's scope.
- **`Usage-Based Pricing — GCP` — considered, rejecting.** Its def says "The GCP equivalent of AWS Subscription pricing," which would union it into the giant Entitlement row (row 2) alongside `SaaS Subscription Pricing — AWS`. But that row's GCP seat is already held by `Subscription — GCP` (an entitlement *record*), while this term describes GCP's PAYG *billing model* — a different dimension. Forcing them into the same row would conflate two different GCP concepts under one cell. Recommend leaving ungrouped, or flagging GCP's own "equivalent" claim as imprecise in a future term-accuracy pass.
- **`Deal Registration — Suger` / `Deal Registration — Azure` — considered, rejecting.** Identical name, and cross-referenced under `Related:` already, but one reviewer flagged a real mechanic mismatch I agree with: Suger's version is a *channel partner registering a deal with the ISV*; Azure's is *the ISV registering a deal with Microsoft*. Same pattern, different relationship layer — grouping them would misrepresent who's registering with whom.
- **Channel/reseller partner program cluster — unresolved, two conflicting groupings proposed.** Candidate terms: `Solution Provider Private Offer (SPPO) — AWS`, `AWS Solution Provider Program (SPP) — AWS`, `Cloud Solution Provider (CSP) — Azure`, `Resale-Enabled Offer — Azure`, `Reseller Program — GCP`. One reviewer grouped `{SPPO, CSP, Reseller Program}`; another grouped `{SPPO, Resale-Enabled Offer}` separately from `{CSP, Reseller Program, SPP}` and flagged that second sub-grouping as "most likely false positive" in their own writeup, since AWS's SPP explicitly says it's "distinct from CPPO/SPPO." I don't have enough confidence to pick a side — recommend a dedicated close read of all five terms' full defs before wiring anything here.
- **`AWS Marketplace List & Sell Incentive — AWS` → Cloud Partner Funding (row 17) — borderline gap-fill.** It's a real cost-offset reimbursement for listing-creation expenses, which the row's GCP member (Partner Innovation Fund) does cover as an explicit use case. But this term isn't tagged `funding` and sits in `operations` not `cosell`, unlike every other member of that row — worth a second look at whether it was deliberately scoped narrower.
- **`Partner Revenue Measurement (PRM) — AWS` / `Partner Reported Azure Consumed Revenue (PRACR) — Azure` — medium confidence.** Both are partner self-reporting frameworks to claim co-sell/funding credit for downstream cloud consumption, but the actual reporting mechanism differs materially (AWS: automated resource tagging; Azure: manual monthly CSV submission). Could go either way — your call.
- **`Request Private Offer — Azure` / `Buy with AWS — AWS` — medium confidence, mixed signal.** Both let a buyer self-trigger a private-offer request instead of a sales conversation, but the surfaces differ (on-listing button vs. off-platform embeddable widget). One reviewer proposed it, another independently considered and declined it.

### Not touched
Everything else in the 234-term singleton set was reviewed and correctly has no cross-platform counterpart — genuinely platform-unique mechanics (e.g. AWS's Concurrent Agreements/LicenseArn model, Azure's CSP eligibility dashboard, most Suger PRM/billing internals). No action needed on those.

### Sign-off
Let me know: "Apply the 8" / "Apply the 8 + [flagged item]" / adjust anything above.
